SET(MODULE_NAME solver)
FILE(GLOB MODULE_SRC *.cpp)
FILE(GLOB MODULE_HDR *.h)
ADD_LIBRARY(${MODULE_NAME} STATIC ${MODULE_SRC} ${MODULE_H})
TARGET_INCLUDE_DIRECTORIES(${MODULE_NAME} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
